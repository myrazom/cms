version: 2.1

orbs:
  node: circleci/node@5.0.2

jobs:
  prepare:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - node/install:
          install-yarn: true
          node-version: '14'
      - node/install-packages:
          pkg-manager: yarn
          cache-path: ~/node_modules/

workflows:
  pr-quality-check:
    jobs:
      - prepare
